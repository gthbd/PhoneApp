package com.yourapp.phoneapp.ui.theme

import androidx.compose.ui.graphics.Color

object HyperColors {
    val Background = Color(0xFF000000)      // nền đen tuyền
    val Surface = Color(0xFF1C1C1E)         // nền search bar, các ô chứa

    val AccentGreen = Color(0xFF34D07F)     // nút gọi / FAB bàn phím
    val AccentBlue = Color(0xFF3482FF)      // dự phòng cho các phần khác sau này

    val TextPrimary = Color(0xFFFFFFFF)
    val TextSecondary = Color(0xFF8E8E93)   // gray cho subtitle, tab chưa chọn
    val TextMuted = Color(0xFF9DA3B8)       // màu chữ cho filter "All calls"

    val MissedCallColor = Color(0xFFFF6B4A) // cam-đỏ cho cuộc gọi nhỡ
    val Divider = Color(0xFF2C2C2E)
}